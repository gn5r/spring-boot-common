{
  "java.configuration.updateBuildConfiguration": "disabled",
  "maven.terminal.favorites": [
    { "alias": "install", "command": "clean install" },
    { "alias": "deploy", "command": "clean deploy" }
  ]
}
